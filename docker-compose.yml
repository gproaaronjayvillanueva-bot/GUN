version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: minimayhem-db
    environment:
      POSTGRES_DB: minimayhem
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - minimayhem-network

  # Redis for matchmaking
  redis:
    image: redis:7-alpine
    container_name: minimayhem-redis
    ports:
      - "6379:6379"
    networks:
      - minimayhem-network

  # Backend Server
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minimayhem-backend
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgresql://postgres:password@postgres:5432/minimayhem
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: minimayhem
      DB_USER: postgres
      DB_PASSWORD: password
      JWT_SECRET: your-super-secret-jwt-key-change-in-production
      JWT_EXPIRES_IN: 7d
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CLIENT_URL: http://localhost:3000
    ports:
      - "3001:3001"
    depends_on:
      - postgres
      - redis
    networks:
      - minimayhem-network
    restart: unless-stopped

  # Frontend Server
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: minimayhem-frontend
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: http://localhost:3001
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - minimayhem-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  minimayhem-network:
    driver: bridge
